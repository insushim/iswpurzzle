/**
 * 빌드 플래그.
 *
 * ⚠️ iap / rewardedAds 를 끈 이유(2026-08-20):
 * 상점의 '💎 젬' 탭은 결제도 광고도 연동된 적이 없다. 버튼을 누르면
 * `alert("실제 결제 연동 필요")` 가 뜨는 게 전부였다 — 즉 기능이 아니라
 * 깨진 UI였고, alert 은 페이지를 통째로 멈춘다.
 * 게다가 이 게임을 초등 학습 플랫폼(킹수학)에 올릴 계획이라면
 * 실결제·보상형 광고 노출 자체가 부적합하다.
 *
 * 실제로 IAP/광고 SDK를 붙이는 날 이 플래그를 켜면 UI가 그대로 돌아온다.
 */
export const FEATURES = {
  /** 인앱 결제(젬 패키지). SDK 연동 전까지 false. */
  iap: false,
  /** 보상형 광고. SDK 연동 전까지 false. */
  rewardedAds: false,
} as const;
