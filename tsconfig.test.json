{
  "//": "테스트 전용 설정 — 계약 테스트가 node:fs로 firestore.rules를 직접 읽기 때문에 node 타입이 필요하다. 앱 코드(tsconfig.app.json)에는 node 타입을 넣지 않는다(브라우저 번들에 node API가 새어 들어가는 걸 막기 위해).",
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",
    "types": [
      "vite/client",
      "node"
    ],
    "lib": [
      "ES2022",
      "DOM",
      "DOM.Iterable"
    ]
  },
  "include": [
    "src/**/*.test.ts",
    "src/**/*.test.tsx"
  ],
  "exclude": []
}
